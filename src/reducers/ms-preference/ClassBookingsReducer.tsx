import ActionType from "../../actions/ActionType";

import { Layout, Table, Divider, Tag, Breadcrumb, Button, Icon } from "antd";

let initState: any = {
};

const classBookingsReducer = (state = initState, action: any) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default classBookingsReducer;